services:
  - type: web
    name: hentaibros-frontend
    runtime: node
    buildCommand: npm install && npm run build && npm install express
    startCommand: node server.js
    envVars:
      - key: BACKEND_URL
        sync: false
      - key: NODE_ENV
        value: production
    autoDeploy: true
